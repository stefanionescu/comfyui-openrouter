#!/usr/bin/env bash
#
# Check Python types.
# Runtime: Bash 3.2+, macOS and Linux.
#MISE description="Check Python types."
set -euo pipefail

# main - Check Python types.
main() {
  REPO_ROOT="${MISE_PROJECT_ROOT:-$(git rev-parse --show-toplevel)}"
  cd "${REPO_ROOT}" || exit 1

  exec uv run --no-sync python -m quality.python.typecheck "$@"
}

main "$@"
